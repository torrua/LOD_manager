# Stable rustfmt options only.
# `imports_granularity` and `group_imports` require nightly — removed to avoid
# "unstable features are only available in nightly channel" warnings on CI.
edition              = "2021"
max_width            = 100
tab_spaces           = 4
newline_style        = "Unix"
use_small_heuristics = "Default"
